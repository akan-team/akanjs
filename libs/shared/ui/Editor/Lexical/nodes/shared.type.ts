/** Media alignment shared by image/video/embed nodes. */
export type MediaAlign = "left" | "center" | "right";
/** CSS object-fit modes offered for images. */
export type ImageFit = "contain" | "cover" | "fill";
